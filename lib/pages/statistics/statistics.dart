import 'package:flutter/material.dart';

class ClimathonStatisticsPage extends StatelessWidget {
  const ClimathonStatisticsPage({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
